if($(window).width() > 991) {
	$('.carousel-image').height($(window).height() - 0);
    $(window).resize(function () {
        $('.carousel-image').height($(window).height() - 0);
    });
    } else {
    $('.carousel-image').height($(window).height() - 0);
    $(window).resize(function () {
        $('.carousel-image').height($(window).height() - 0);
    });
}